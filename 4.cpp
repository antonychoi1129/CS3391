#include<iostream>
#include<vector>
#include<string>
#include<queue>
#include<algorithm>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int L, W, N;
    int i, j, k, cases = 0;
    int x[105], y[105];
    while(cin >> L >> W >> N){
        int L, W, N, d;
        for(i = 0; i < N; i++)
            cin >> x[i] >> y[i];
        int start = 2 * N, end = 2 * N + 1;
    }
    return 0;
} 